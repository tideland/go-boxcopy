name: build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check out local dependencies
        run: |
          git clone --depth=1 https://github.com/tideland/go-asserts.git ../asserts
          git clone --depth=1 https://github.com/tideland/go-toml.git   ../toml
          git clone --depth=1 https://github.com/tideland/go-wait.git   ../wait
          git clone --depth=1 https://github.com/tideland/go-worker.git ../worker

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: 1.24

      - name: Install golangci-lint
        run: |
          curl -sSfL https://raw.githubusercontent.com/golangci/golangci-lint/master/install.sh | sh -s -- -b $(go env GOPATH)/bin v2.7.2
          echo "$(go env GOPATH)/bin" >> $GITHUB_PATH

      - name: Lint
        run: golangci-lint run --timeout=5m

      - name: Build
        run: go build -v ./...

      - name: Test
        run: go test -v -race ./...
